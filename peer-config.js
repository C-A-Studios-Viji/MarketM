// MarketM multiplayer signalling configuration.
// Empty object = free PeerJS Cloud signalling service.
// To use the included self-hosted PeerServer, replace this with e.g.:
// window.MARKETM_PEER_OPTIONS = { host: 'your-backend.example.com', port: 443, path: '/marketm', secure: true, key: 'marketm' };
window.MARKETM_PEER_OPTIONS = {};
